library(devtools)
#create_package("~/Desktop/square")
use_git()
usethis::use_github()

use_r("square")
      #square <- function(x) {
      #  x^2
      #}

load_all()
square(10)
square(c(2, 10))
check()

dplyr::
use_mit_license("Jaden Phillips")

#Edit description of function
# insert Roxygen Skelecton (under "Code" tab in R studio)
document()
?square
check()
install()

library(square)
square::square(5)
#To declare that you're going to write formal unit tests for your function:
use_testthat()
use_test()

use_readme_rmd() #when at last creating readme.RMD file for entire directory
devtools::build_readme()

x_list <- as.list(c(1,2,3))
#square(x_list)   fails because x_list is not a numeric value
purrr::map_dbl(x_list, square) #this requires another package that the user may not have
use_package("purrr")

purrr::map_dbl(function_argument, function(x) x^2)


use_vignette("using_square")
build_vignettes()



# How to add data into our R packages:
patients <- data.frame(id = 1:3, age = 15:17)
use_data(patients)

use_news_md()
